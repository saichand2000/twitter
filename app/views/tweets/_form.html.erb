<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %> 

  <div class="field container mt-4 mb-5">
    <%= form.label :tweet,class:"p-2 px-3" %></br>
    <%= form.text_area :tweet,class:"p-2 px-3",placeholder:"New Tweet" %>
  </div>

 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
